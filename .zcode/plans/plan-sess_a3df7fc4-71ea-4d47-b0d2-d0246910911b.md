# Port the Preview Design + Stress Test + Micro-Level Security Audit

## Context
The preview URL is a deployed evolution of this same codebase. Its Turbopack dev bundle contained complete compiled sources of every redesigned component — including a complete `tools.tsx` (locally corrupted). I extracted all 23 modules, the exact hex design tokens, the full new bilingual copy, and the utility CSS. The preview preserves all security behaviors (honeypot, 429 handling, IO-deferred ETA fetch), so porting is behavior-safe. Per your earlier answer: **4 calculator tabs** (E-Invoice diagnostic removed, following the preview).

## Implementation order (all files under `src/`)
1. **`app/globals.css`** — replace palette with preview tokens (light `#f8f7ee`/`#176749`/gold `#e9c260`; dark `#06120d`/`#71c59f`; deep `#042319`/`#00150d`/`#083124`; radius 1rem; 44px gridpattern; new gradient/animation timings). Keep `--deep-4`/`--gold-light` mapped to the new palette.
2. **`lib/site-content.ts`** — replace ONLY the `content` AR+EN copy with the preview copy (verified key-identical to the existing `Content` type). **Keep the local hardened engines** (`computeDelayFine` with input validation + loop caps, `computeIncomeTax` with caps — the preview versions dropped those guards), keep `CONTACT`/`ETA`/constants.
3. **Rebuild 15 components** from the extracted sources as clean TSX: `primitives`, `logo`, `chrome` (Topbar/Header/Footer), `hero` (new Ring SVG + orbital dashboard), `services`, `why` (Process/Journey), `proof` (Testimonials/Insights), `tools` (**complete 4-tab Calculators — fixes the corruption**), `eta-panel`, `eta-section`, `about`, `faq`, `contact` (new CtaBanner), `offer-popup`. `lang-provider` untouched.
4. **`app/layout.tsx`** — themeColor `#f8f7ee` / `#12211d` (preview values).
5. **Untouched**: all API routes, db/mail/rate-limit/errors/logger/phone libs, `next.config.ts` (CSP/headers), prisma schema, DB rows.

## Verification & testing
- `bun x tsc --noEmit` + lint → zero errors
- Dev server on 127.0.0.1:3000 → browser check AR/EN × light/dark, anchors, calculator math, ETA panel, contact submit
- **Stress tests** (local Node scripts): contact limiter (5/min admits, 6th→429 with retryAfterSec), 50KB body→413, invalid JSON, honeypot fake-success, eta-news 30-concurrent (cache + 10/min limit + graceful upstream failure), rate-limiter key-flood (10k keys → bounded memory/eviction), p50/p95 latency
- **Micro security audit** (every file): Zod regex bypass vectors, XSS/injection in rendered data, `javascript:` URL defense (isValidSlug), rel=noopener coverage, CSP/headers intact, no PII in logs/localStorage, dependency advisories — fix everything found and re-verify.